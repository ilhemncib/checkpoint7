function pal(str)
 {
    if 
       (str.length <=1)
         return "str est palandrome"
        else if 
        (str[0] <> str [str.length-1])
        return "str non palandrome"
        else 
        return pal(str.slace (0, str.length))
    end 
}
end